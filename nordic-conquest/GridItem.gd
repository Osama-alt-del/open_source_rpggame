extends Node
# Node : standard 2d object library

# Make a class:
# with this, GODOT now knows that the whole file is a class 
class_name GridItem 

# extends from <Base Class>  # This is for inheritance

var x = 0
var y = 0

# Init
func _ready() -> void:
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
# Basically Game loop
